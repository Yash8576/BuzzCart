# Database Configuration (PostgreSQL)
DATABASE_URL=postgres://like2share_user:like2share_dev_password@localhost:5432/like2share_db?sslmode=disable

# Firebase Cloud SQL via Auth Proxy example
# DATABASE_URL=postgres://buzzcart_app:REPLACE_APP_PASSWORD@127.0.0.1:5432/buzzcart-daeb6-database?sslmode=disable

# JWT Configuration
JWT_SECRET=buzz-social-cart-secret-key-2024

# Server Configuration
PORT=8000
GIN_MODE=debug

# MinIO Configuration (S3 Compatible Storage)
MINIO_ENDPOINT=localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin123
MINIO_USE_SSL=false
MINIO_BUCKET=buzzcart-media

# OpenAI Configuration (optional)
OPENAI_API_KEY=
